import React from "react";

import Section from "@components/Section";

const Experience: React.FC = ({ data }) => {
  return (
    <Section
      id="experience"
      title="Experience"
      className="dark"
      skew
      orientation={["top", "left"]}
    >
      <ul className="experience">

      <ul>

    </Section>
  );
};

export default Experience;
